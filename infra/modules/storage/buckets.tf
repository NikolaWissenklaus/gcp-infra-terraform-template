# Use o trecho abaixo para criar um bucket
/*----------------------------------------------------

resource "google_storage_bucket" "sera-tiuuu" {
  name                        = var.local.variavel_nome_bucket
  location                    = var.region
  force_destroy               = false
  uniform_bucket_level_access = true
}

----------------------------------------------------*/

